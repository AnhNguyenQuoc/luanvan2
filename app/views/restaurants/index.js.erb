<% if @restaurants.count > 0 %>
$('#restaurants').html("<%= escape_javascript(render partial: 'restaurant_item', collection: @restaurants, as: :restaurant ) %>")
<% else %>
$('#restaurants').html("Hiện không có cửa hàng bạn tìm kiếm")
<% end %>
