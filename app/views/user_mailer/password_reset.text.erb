Để reset mật khẩu của bạn, nhấn vào link bên dưới:

<%= edit_password_reset_url(@user.reset_token, email: @user.email) %>

Link sẽ tồn tại trong vòng 2 tiếng trước khi mất hiệu lực.

Nếu đây không phải là yêu cầu của bạn thì hãy bỏ qua nó.